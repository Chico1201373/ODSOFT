version: '3.8'

services:
  books-api:
    image: chico0706/books-api:latest
    build: .
    container_name: books-api-latest
    ports:
      - "8081:8081"
    networks:
      - test-network
    restart: unless-stopped
